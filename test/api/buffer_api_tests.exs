defmodule Quillex.APITests.BufferAPITest do
  use ExUnit.Case
  # alias Quillex.Structs.BufState

  # test "make a new Buffer" do
  #   new_buf = Buffer.new(%{id: {:buffer, "luke_buf"}})

  # assert new_buf == %Quillex.Structs.BufState{
  #            id: {:buffer, "luke_buf"},
  #            name: "luke_buf",
  #            type: :text,
  #            data: "",
  #            cursors: [%Quillex.Structs.BufState.Cursor{num: 1, line: 1, col: 1}],
  #            history: [],
  #            scroll_acc: {0, 0},
  #            read_only?: false
  #          }
  # end

  test "when the app boots, there's one Buffer open" do

  end
end
